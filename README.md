# Lab 7

## Check Your Understanding Answer

1. I select answer 1. I would fit my automated test in the Recipe project development pipeline within
a Github Action that runs whenever the code is pushed because it automatically checks for errors and
prevents broken code from being merged into the project. This makes development more reliable and helps teams catch bugs early.

2. No, I would use unit test to check if a function returns the correct output. E2E testing is for
emulating user actions, not for individual functions.

3. Navigation mode analyzes a page after it loads, so it focus on load performance. While snapshot mode
analyzes a page at it current state; it cannot measure performance.

4. Three things that we can improve: Minify javascript, reduce unused javascript, use efficient cache lifetimes







